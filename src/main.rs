// CELL
mod advanced_channels;
mod automaton;
mod commands;
mod game_of_life;
mod universe;
// mod heat_dispersion;
// mod terminal_ui;
// use automaton::CellularAutomaton;
// use simulator::Simulator;
// use terminal_ui::TerminalUI;

fn main() -> () {
    // let instance = Instance::new(None, &InstanceExtensions::none(), None).unwrap();
    // let automaton = CellularAutomaton::new("Conway's Game Of Life", &MOORE_NEIGHBORHOOD);
    // let sim = Simulator::new_gpu_sim(automaton, &game_of_life::gosper_glider_gun(), instance);
    // let mut term_ui = TerminalUI::new(sim);
    // term_ui.cmd_interpreter().unwrap();
}
